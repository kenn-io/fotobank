name: CI

# The PR dispatcher always calls the main-pinned workflow. That workflow
# independently routes same-repository Linux jobs to the managed private fleet
# and fork jobs to GitHub-hosted runners.
on:
  pull_request:

permissions: read-all

jobs:
  run:
    uses: kenn-io/fotobank/.github/workflows/ci.yml@main
