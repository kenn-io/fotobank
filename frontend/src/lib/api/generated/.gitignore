# Schema is regenerated via `make api-generate` from openapi.json.
# We commit it so the frontend builds without a Go toolchain available;
# the .gitignore here just documents that everything else under this
# directory is generated and shouldn't be hand-edited.
*
!.gitignore
!.gitkeep
!schema.ts
