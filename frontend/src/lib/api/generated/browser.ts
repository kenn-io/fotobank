/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Fotobank
 * Fotobank HTTP API
 * OpenAPI spec version: dev
 */
export type DownloadMediaThumbParams = {
size?: DownloadMediaThumbSize;
/**
 * @minimum 0
 */
v: number;
};

export type DownloadMediaThumbSize = typeof DownloadMediaThumbSize[keyof typeof DownloadMediaThumbSize];


export const DownloadMediaThumbSize = {
  grid: 'grid',
  preview: 'preview',
  large: 'large',
} as const;

export const getEventsUrl = () => {




  return `/api/v1/events`
}

/**
 * Server-sent events scoped to the caller. Use EventSource with the generated URL for browser reconnection and Last-Event-ID handling.
 * @summary Subscribe to photo library events
 */
export const events = async ( options?: RequestInit): Promise<string> => {

  const res = await fetch(getEventsUrl(),
  {
    ...options,
    method: 'GET'


  }
)

  const contentType = (res.headers.get('content-type') ?? '').toLowerCase();
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: string = body ? (contentType.includes('json') ? JSON.parse(body) : body) : {}
  return data
}



export const getDownloadMediaFileUrl = (id: string,
    fileID: string,) => {




  return `/api/v1/media/${encodeURIComponent(String(id))}/files/${encodeURIComponent(String(fileID))}/content`
}

/**
 * Returns the owned file's current bytes with its stored media type. Hidden media requires an owner-matched unlock session. A file attached to a different asset returns 404. Use media details to obtain the expected size and SHA-256, then verify a complete download before using it; concurrent edits can invalidate those details.
 * @summary Read original file bytes
 */
export const downloadMediaFile = async (id: string,
    fileID: string, options?: RequestInit): Promise<Blob> => {

  const res = await fetch(getDownloadMediaFileUrl(id,fileID),
  {
    ...options,
    method: 'GET'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.blob();
  const data: Blob = body as Blob
  return data
}



export const getDownloadMediaOriginalUrl = (id: string,) => {




  return `/api/v1/media/${encodeURIComponent(String(id))}/original`
}

/**
 * Returns the owned file's current bytes with its stored media type. Hidden media requires an owner-matched unlock session. A file attached to a different asset returns 404. Use media details to obtain the expected size and SHA-256, then verify a complete download before using it; concurrent edits can invalidate those details.
 * @summary Read original file bytes
 */
export const downloadMediaOriginal = async (id: string, options?: RequestInit): Promise<Blob> => {

  const res = await fetch(getDownloadMediaOriginalUrl(id),
  {
    ...options,
    method: 'GET'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.blob();
  const data: Blob = body as Blob
  return data
}



export const getDownloadMediaThumbUrl = (id: string,
    params: DownloadMediaThumbParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v1/media/${encodeURIComponent(String(id))}/thumb?${stringifiedParams}` : `/api/v1/media/${encodeURIComponent(String(id))}/thumb`
}

/**
 * @summary Read a versioned media thumbnail
 */
export const downloadMediaThumb = async (id: string,
    params: DownloadMediaThumbParams, options?: RequestInit): Promise<Blob> => {

  const res = await fetch(getDownloadMediaThumbUrl(id,params),
  {
    ...options,
    method: 'GET'


  }
)


  const body = [204, 205, 304].includes(res.status) ? null : await res.blob();
  const data: Blob = body as Blob
  return data
}
