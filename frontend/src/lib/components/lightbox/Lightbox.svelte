<!-- frontend/src/lib/components/lightbox/Lightbox.svelte -->
<!-- Temporary stub — replaced in Task 13. -->
<script lang="ts">
  // The dispatcher passes id, from, and four stores. The stub accepts
  // them as opaque rest props so the typecheck passes; T13 replaces
  // this file with a typed implementation.
  let { id }: { id: string; from: string; [k: string]: unknown } = $props();
</script>
<div class="lb-stub">Lightbox stub for {id}</div>
